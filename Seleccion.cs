/*namespace Deber02_Ordenamiento;
//namespace System.Diagnostics;

class Program
{
    static void Main2(string[] args)
    {
        /*Stopwatch tiempo = new Stopwatch();
        tiempo.Start();


        int[] A = { 51, 21, 39, 80, 36 };
        Console.WriteLine("\nArreglo desordenado: ");
        for (int i = 0; i < A.Length; i++)
        {
            Console.Write($"A[{i}]={A[i]}, ");
        }

        ordenSeleccion(A, A.Length);
        Console.WriteLine("\n\nArreglo ordenado por seleccion:");
        for (int i = 0; i < A.Length; i++)
        {
            Console.Write($"A[{i}]={A[i]}, ");
        }
        Console.WriteLine("\n\n ");
        /*tiempo.Stop();
        Console.WriteLine("Tiempo de ejecución: "+ tiempo.Elapsed.TotalMilliseconds);


        static void ordenSeleccion(int[] A, int n)
        {
            int indiceMenor, aux = 0;

            Console.Write("\n\nComienzo:                                                   Cambio:        Pasadas:");
            Console.WriteLine("\n");

            for (int i = 0; i < n - 1; i++)
            {
                indiceMenor = i;
                for (int j = i + 1; j < n; j++)
                {
                    if (A[j] < A[indiceMenor])
                    {
                        indiceMenor = j;
                    }
                }
                aux = A[i];
                A[i] = A[indiceMenor];
                A[indiceMenor] = aux;

                for (int k = 0; k < n; k++)
                {
                    Console.Write($"A[{k}]={A[k]}, ");
                }
                Console.WriteLine("          " + $"A[{i}]={A[indiceMenor]}" + $"  A[{i}]={A[i]},       " + i);
            }

        }
    }
}*/
