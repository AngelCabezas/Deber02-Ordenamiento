/*namespace Deber02_Ordenamiento;
//namespace System.Diagnostics;

class Program
{
    static void Main3(string[] args)
    {
      /*  Stopwatch tiempo = new Stopwatch();
        tiempo.Start();


        int[] A = { 51, 21, 39, 80, 36 };
        Console.WriteLine("\nArreglo desordenado: ");
        for (int i = 0; i < A.Length; i++)
        {
            Console.Write($"A[{i}]={A[i]}, ");
        }

        ordenInsercion(A, A.Length);
        Console.WriteLine("\n\nArreglo ordenado por Insercion:");
        for (int i = 0; i < A.Length; i++)
        {
            Console.Write($"A[{i}]={A[i]}, ");
        }
        Console.WriteLine("\n\n ");
        /*tiempo.Stop();
        Console.WriteLine("Tiempo de ejecución: " + tiempo.Elapsed.TotalMilliseconds);


        static void ordenInsercion(int[] A,int n)
        {
            Console.Write("\n\nComienzo:\n");
            for (int i = 1; i < n; i++)
            {
                int key = A[i];
                int j = i - 1;
                while (j >= 0 && A[j] > key)
                {
                    A[j + 1] = A[j];
                    j = j - 1;
                }
                A[j + 1] = key;

                for (int k = 0; k < n; k++)
                {
                    Console.Write($"A[{k}]={A[k]}, ");
                }
                Console.Write("\n");
            }
        }
    }
}*/
